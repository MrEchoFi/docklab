[package]
name = "docklab"
version = "0.1.0"
edition = "2021"
description = "A CLI tool to spin up and manage a Kali Linux Docker lab"

[[bin]]
name = "lab"
path = "src/main.rs"

[dependencies]
clap = { version = "=4.4.18", features = ["derive"] }
crossterm = "0.29.0"
ratatui = "0.30.2"